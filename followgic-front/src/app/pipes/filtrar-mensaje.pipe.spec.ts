import { FiltrarMensajePipe } from './filtrar-mensaje.pipe';

describe('FiltrarMensajePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarMensajePipe();
    expect(pipe).toBeTruthy();
  });
});
