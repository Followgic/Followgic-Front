import { FiltrarStringPipe } from './filtrar-string.pipe';

describe('FiltrarStringPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarStringPipe();
    expect(pipe).toBeTruthy();
  });
});
