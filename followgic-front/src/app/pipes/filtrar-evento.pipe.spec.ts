import { FiltrarEventoPipe } from './filtrar-evento.pipe';

describe('FiltrarEventoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarEventoPipe();
    expect(pipe).toBeTruthy();
  });
});
